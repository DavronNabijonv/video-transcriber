services:
  - type: web
    name: video-transcriber
    env: python
    buildCommand: apt-get install -y ffmpeg && pip install -r requirements/server.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
