services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Persist downloaded Whisper models across container restarts
      - whisper_cache:/root/.cache/whisper
    restart: unless-stopped

volumes:
  whisper_cache:
